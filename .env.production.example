# Production build on same domain as y_fi_backend (/var/www/y_fi_frontend)
# Copy to .env on the droplet before: sudo bash deploy/deploy.sh
#
# /admin on this domain is Django admin (nginx → y_fi_backend), not Theme Studio.

VITE_APP_API_BASE=https://app.freeyfi.com/api
