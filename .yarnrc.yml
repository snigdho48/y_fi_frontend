# Use classic node_modules — Vite/esbuild on Windows cannot resolve Yarn PnP virtual paths reliably.
nodeLinker: node-modules

approvedGitRepositories:
  - "**"

enableScripts: true

npmMinimalAgeGate: 0
