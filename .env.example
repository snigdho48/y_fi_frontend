# Admin Theme Studio connects ONLY to dashboard-backend
# Production (same domain as y_fi_backend SPA root):
# VITE_DASHBOARD_API_BASE=https://dashboard.freeyfi.com/api
VITE_DASHBOARD_API_BASE=http://127.0.0.1:8001/api

# Marketing contact form — same-origin API when SPA + backend share the domain:
# VITE_APP_API_BASE=https://app.freeyfi.com/api
# Local override:
# VITE_APP_API_BASE=http://127.0.0.1:8000/api
