# Local development (.env). Production droplet uses .env.production.example → .env

# Same-origin / main API (y_fi_backend) — contact form, APK links, etc.
# VITE_APP_API_BASE=http://127.0.0.1:8000/api

# Optional: Theme Studio only when running dashboard-backend locally
# VITE_DASHBOARD_API_BASE=http://127.0.0.1:8001/api
